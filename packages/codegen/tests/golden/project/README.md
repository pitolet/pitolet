# Welcome

Exported from Pitolet.

- `theme.css` contains the Tailwind v4 theme.
- `frames/` contains one React component for each frame.

This export requires Tailwind CSS v4. Import `theme.css` before using the components; it already includes `@import 'tailwindcss'`.
