# Pitolet vs Figma page

The live copy is maintained in `site/build.ts`. Run `pnpm build:site` after
changing it, then check the result with:

```sh
pnpm check:site
pnpm qa:site
```

The generated page is written to `deploy/static/vs-figma/index.html`.
